<!DOCTYPE html>
<html>
  <head>
    <title>Twitter Clone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    
    <%= javascript_importmap_tags %>
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
  </head>

  <body class="bg-gray-100">
    <nav class="bg-blue-500 p-4">
      <div class="container mx-auto flex justify-between items-center">
        <%= link_to "Twitter Clone", root_path, class: "text-white text-2xl font-bold" %>
        <div class="space-x-4">
          <% if current_user %>
            <%= link_to "Timeline", timeline_path, class: "text-white" %>
            <%= link_to "Profile", profile_path(username: current_user.username), class: "text-white" %>
            <%= button_to "Sign Out", logout_path, method: :delete, class: "text-white bg-transparent border-0 cursor-pointer" %>
          <% else %>
            <%= link_to "Sign In", login_path, class: "text-white" %>
            <%= link_to "Sign Up", signup_path, class: "text-white" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-4 px-4">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
